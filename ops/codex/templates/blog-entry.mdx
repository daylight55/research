---
title: '<REPORT_TITLE>'
description: '<ONE_SENTENCE_DESCRIPTION>'
rssSummary: '<RSS_SHARE_SUMMARY>'
pubDate: '<YYYY-MM-DD>'
heroImage: '../../../../src/assets/images/placeholder-hero.jpg'
heroImageQuery: '<SPECIFIC_UNSPLASH_SEARCH_WORDS_OR_COMMA_SEPARATED_ALTERNATES>'
heroImageAlt: '<HERO_IMAGE_ALT_TEXT>'
category: '<CATEGORY_NAME>'
tags: ['<TAG_1>', '<TAG_2>']
draft: false
---

<!--
Daily research site entry template.

Write the full article body directly in this MDX file after the frontmatter.
Do not maintain a separate `report.md` copy, and do not import Markdown as a
component. Imported Markdown renders the body, but Astro does not expose
imported headings through `post.render().headings`, so the article
table-of-contents sidebar becomes empty. If the investigation process should be
public, summarize it in `research-log.mdx` next to this `index.mdx`.

Keep Mermaid fences in the article body. Use `<SourceNote>...</SourceNote>`
near the claim it supports instead of plain `出典メモ:` paragraphs. `SourceNote`
is registered by the post renderer, so do not import it in ordinary report MDX.
Write each `SourceNote` on one line, for example:
`<SourceNote>[Source name](https://example.com) に基づく。</SourceNote>`.
Do not put the opening/closing tags on separate lines, and do not write manual
parentheses, `出典:`, `Source note:`, or `Source:` inside it; the component
renders that wrapper.
-->

# <REPORT_TITLE>

## 1. エグゼクティブサマリー
