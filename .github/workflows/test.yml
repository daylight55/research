name: Test

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: test-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test and build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check diff whitespace
        run: git diff --check

      - name: Run repository tests
        run: pnpm test

      - name: Validate changed mixed article alignment
        if: github.event_name == 'pull_request'
        run: node ops/scripts/validate-mix-alignment.mjs --changed origin/${{ github.base_ref }}...HEAD

      - name: Validate all mixed article alignment
        run: node ops/scripts/validate-mix-alignment.mjs

      - name: Build site
        run: pnpm build
