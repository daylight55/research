---
import LocaleToggle from '@/components/LocaleToggle'
import { DEFAULT_LOCALE, type Locale } from '@/utils'

const { locale = DEFAULT_LOCALE } = Astro.props as { locale?: Locale }
---

<LocaleToggle locale={locale} variant='floating' />
